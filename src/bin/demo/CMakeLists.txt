set(APPNAME ${DEMO_APP_NAME})

set(APPLICATION_NAME ${DEMO_APP_NAME})
set(APPLICATION_VERSION "1.0" )

configure_file(config.hxx.in config.hxx)

set(CMAKE_CXX_FLAGS "${PROJECT_COMPILER_FLAGS}")
SET(CMAKE_AUTOMOC ON)

set(HEADER_FILES
)

set(SOURCE_FILES
    main.cxx
)

add_executable(${APPNAME} ${HEADER_FILES} ${SOURCE_FILES})

target_link_libraries(${APPNAME}
    ${WIDGETS_LIBRARY_NAME}
    ${APP_LIBRARY_NAME}
)

add_test(NAME ${APPNAME} COMMAND ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/${APPNAME})
